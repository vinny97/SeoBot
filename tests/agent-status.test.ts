import { describe,expect,it } from "vitest";
import { deriveAgentStatus } from "../lib/search-intelligence/agent-status";
const state=(overrides:Partial<Parameters<typeof deriveAgentStatus>[0]>={})=>deriveAgentStatus({jobs:[],connectionStatus:null,hasBestRecommendation:false,hasApprovedWork:false,hasError:false,...overrides});
describe("agent status",()=>{it("derives active work from real job types",()=>{expect(state({jobs:[{jobType:"website_crawl",status:"running"}]})).toBe("analysing_website");expect(state({jobs:[{jobType:"gsc_initial_sync",status:"queued"}]})).toBe("syncing_search_data");expect(state({jobs:[{jobType:"generate_seo_recommendations",status:"running"}]})).toBe("comparing_opportunities")});it("waits for the user only when a best recommendation exists",()=>expect(state({connectionStatus:"connected",hasBestRecommendation:true})).toBe("waiting_for_user"));it("prioritises errors and measurement states",()=>{expect(state({hasError:true,hasBestRecommendation:true})).toBe("error");expect(state({hasApprovedWork:true})).toBe("measuring")})});
