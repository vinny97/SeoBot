import { NextResponse } from "next/server";import { z } from "zod";import { readCrawlRun } from "@/lib/data/crawl-runs";export const dynamic="force-dynamic";
export async function GET(_:Request,{params}:{params:Promise<{crawlRunId:string}>}){const {crawlRunId}=await params;if(!z.string().uuid().safeParse(crawlRunId).success)return NextResponse.json({error:"Invalid crawl identifier."},{status:400});const run=await readCrawlRun(crawlRunId);return run?NextResponse.json(run,{headers:{"Cache-Control":"private, no-store"}}):NextResponse.json({error:"Crawl not found."},{status:404})}
