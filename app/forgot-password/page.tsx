"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, CheckCircle2 } from "lucide-react";
import { Button, Input } from "@/components/ui";
export default function ForgotPasswordPage(){const [sent,setSent]=useState(false);return <main className="grid min-h-screen place-items-center px-5"><div className="w-full max-w-md rounded-3xl border border-[var(--border)] bg-[var(--card)] p-7 shadow-sm"><Link href="/login" className="focus-ring mb-7 inline-flex items-center gap-2 rounded-lg text-sm text-[var(--muted)]"><ArrowLeft size={16}/>Back to login</Link>{sent?<div role="status" className="text-center"><CheckCircle2 className="mx-auto text-[var(--success)]" size={42}/><h1 className="mt-4 text-2xl font-semibold">Preview complete</h1><p className="mt-2 text-[var(--muted)]">No email was sent. Password recovery will be connected with authentication in a later milestone.</p></div>:<><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--accent)]">Visual placeholder</p><h1 className="mt-2 text-2xl font-semibold">Reset your password</h1><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Preview the future recovery flow. This does not send an email.</p><form onSubmit={(event)=>{event.preventDefault();setSent(true)}} className="mt-6 space-y-4"><Input label="Email address" type="email" required/><Button size="lg" className="w-full">Preview reset request</Button></form></>}</div></main>}
