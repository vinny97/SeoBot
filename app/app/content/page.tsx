"use client";
import { FileText } from "lucide-react";
import { useDemo } from "@/components/demo-provider";
import { SetupNotice } from "@/components/foundation";
import { Badge, Button, Card, PageHeader } from "@/components/ui";
import { createContentHypotheses } from "@/lib/mock/dashboard";
export default function ContentPage(){const {data,statuses,setDemoStatus}=useDemo();const items=createContentHypotheses(data);return <><PageHeader eyebrow="Content foundation" title="Content opportunities" description="Potential ideas shaped by your business profile. No full articles are generated."/><SetupNotice title="Initial hypotheses only" description="These examples demonstrate a future planning workflow and do not call an AI model."/><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(item=>{const status=statuses[item.id]||"Initial hypothesis";return <Card key={item.id} className="flex flex-col p-5"><div className="flex items-start justify-between gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[var(--accent-soft)] text-[var(--accent)]"><FileText size={19}/></span><Badge tone={status==="Dismissed"?"red":status==="Planned"?"blue":"amber"}>{status}</Badge></div><h2 className="mt-4 font-semibold">{item.title}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{item.purpose}</p><dl className="mt-4 flex-1 space-y-2 text-xs text-[var(--muted)]"><div><dt className="inline font-semibold text-[var(--foreground)]">Type: </dt><dd className="inline">{item.contentType}</dd></div><div><dt className="inline font-semibold text-[var(--foreground)]">Customer: </dt><dd className="inline">{item.customer}</dd></div><div><dt className="inline font-semibold text-[var(--foreground)]">Source: </dt><dd className="inline">{item.source}</dd></div></dl><div className="mt-5 flex gap-2 border-t border-[var(--border)] pt-4">{status==="Dismissed"?<Button size="sm" variant="secondary" onClick={()=>setDemoStatus(item.id,"Initial hypothesis")}>Restore</Button>:<><Button size="sm" variant="secondary" onClick={()=>setDemoStatus(item.id,"Planned")}>Plan</Button><Button size="sm" variant="ghost" onClick={()=>setDemoStatus(item.id,"Dismissed")}>Dismiss</Button></>}</div></Card>})}</div></>}
