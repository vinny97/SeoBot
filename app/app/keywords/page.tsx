import { Search } from "lucide-react";
import { Badge, Card, PageHeader } from "@/components/ui";
import { SetupNotice } from "@/components/foundation";
import { topicHypotheses } from "@/lib/mock/dashboard";
export default function KeywordsPage(){return <><PageHeader eyebrow="Foundation" title="Keyword and topic opportunities" description="This area will use verified keyword and first-party data after real sources are connected."/><SetupNotice title="Examples only" description="The topics below are clearly labelled hypotheses. No search volume, ranking or traffic data is being estimated."/><Card className="mt-5 divide-y divide-[var(--border)]">{topicHypotheses.map(item=><div key={item.topic} className="grid gap-3 p-5 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div className="flex items-center gap-3"><Search size={18} className="text-[var(--accent)]"/><div><h2 className="font-semibold">{item.topic}</h2><p className="mt-1 text-xs text-[var(--muted)]">Initial hypothesis</p></div></div><Badge>{item.intent}</Badge><Badge tone={item.relevance==="High"?"green":"amber"}>{item.relevance}</Badge></div>)}</Card></>}
