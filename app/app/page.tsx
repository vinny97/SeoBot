"use client";
import Link from "next/link";
import { ArrowRight, CalendarClock, CheckCircle2, CircleDot, Clock3, Plug, ShieldCheck, Sparkles } from "lucide-react";
import { Badge, Card, EmptyState, LinkButton } from "@/components/ui";
import { useProject } from "@/components/project-provider";
import { formatRelative } from "@/lib/utils";

export default function Dashboard(){const {project}=useProject();const running=project.jobs.filter(j=>j.status==="Running");const upcoming=project.jobs.filter(j=>j.status==="Queued");const completed=project.activities.filter(a=>a.status==="completed").slice(0,4);const opportunities=project.opportunities.filter(o=>o.status!=="Dismissed").slice(0,3);const hour=new Date().getHours();const greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening";return <><header className="mb-9"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-[var(--accent)]">{greeting}.</p><h1 className="mt-1 text-3xl font-semibold tracking-[-.035em]">Here’s what your SEO employee is working on.</h1><p className="mt-2 text-[var(--muted)]">{project.website.domain} · Initial plan in progress</p></div><div className="flex items-center gap-2 rounded-full border border-[var(--line)] bg-[var(--surface)] px-3 py-2 text-sm"><span className="h-2 w-2 animate-pulse rounded-full bg-[var(--accent)]"/><strong>Active</strong><span className="text-[var(--muted)]">· updated {formatRelative(project.updatedAt)}</span></div></div></header><section><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--accent)]">Current work</p><h2 className="mt-1 text-xl font-semibold">In progress now</h2></div><Link href="/app/activity" className="text-sm font-semibold text-[var(--accent)]">View activity</Link></div>{running.length?running.map(job=><Card key={job.id} className="p-5 sm:p-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><CircleDot size={23}/></div><div className="flex-1"><div className="flex items-center gap-2"><h3 className="font-semibold">{job.title}</h3><Badge tone="green">Working</Badge></div><p className="mt-1 text-sm text-[var(--muted)]">{job.description}</p><div className="mt-4 flex items-center gap-3"><div className="h-2 flex-1 overflow-hidden rounded-full bg-[#e6e6e0]"><div className="h-full rounded-full bg-[var(--accent)]" style={{width:`${job.progress||0}%`}}/></div><span className="text-xs font-semibold text-[var(--muted)]">{job.progress}%</span></div></div><div className="text-xs text-[var(--muted)]">Started {formatRelative(job.startedAt)}</div></div></Card>):<EmptyState title="No work in progress" description="New work will appear here when a job starts."/>}</section><div className="mt-9 grid gap-7 xl:grid-cols-[1.25fr_.75fr]"><div className="space-y-8"><section><div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-semibold">Opportunities worth knowing about</h2><Link href="/app/opportunities" className="text-sm font-semibold text-[var(--accent)]">View all</Link></div><div className="space-y-3">{opportunities.map(o=><Link href="/app/opportunities" key={o.id} className="block"><Card className="group p-5 transition hover:border-[#b9c7c0]"><div className="flex items-start gap-4"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[#f0eee6] text-[var(--accent)]"><Sparkles size={18}/></span><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold">{o.title}</h3><Badge>{o.impact} impact</Badge></div><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{o.why}</p></div><ArrowRight size={17} className="mt-2 text-[var(--muted)] transition group-hover:translate-x-1"/></div></Card></Link>)}</div></section><section><h2 className="mb-4 text-xl font-semibold">Completed work</h2><Card className="divide-y divide-[var(--line)]">{completed.map(a=><div key={a.id} className="flex gap-3 p-4"><CheckCircle2 size={19} className="mt-0.5 shrink-0 text-[var(--accent)]"/><div className="flex-1"><p className="text-sm font-semibold">{a.title}</p><p className="mt-0.5 text-sm text-[var(--muted)]">{a.description}</p></div><span className="text-xs text-[var(--muted)]">{formatRelative(a.timestamp)}</span></div>)}</Card></section></div><aside className="space-y-6"><section><h2 className="mb-4 text-xl font-semibold">Approvals</h2><Card className="p-5"><EmptyState icon={<ShieldCheck size={20}/>} title="Nothing needs your approval" description="Drafts and proposed page changes will appear here before anything moves forward."/></Card></section><section><h2 className="mb-4 text-xl font-semibold">Up next</h2><Card className="divide-y divide-[var(--line)]">{upcoming.map(job=><div key={job.id} className="flex gap-3 p-4"><Clock3 size={18} className="mt-0.5 shrink-0 text-[var(--muted)]"/><div><p className="text-sm font-semibold">{job.title}</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">{job.description}</p></div></div>)}</Card></section><Card className="border-[#cbd9d2] bg-[var(--accent-soft)] p-5"><Plug size={21} className="text-[var(--accent)]"/><h3 className="mt-3 font-semibold">Use real search performance</h3><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Search Console will replace estimates and hypotheses with real impressions, queries and clicks.</p><LinkButton href="/app/integrations" variant="secondary" className="mt-4">View planned integrations</LinkButton></Card></aside></div></>}
