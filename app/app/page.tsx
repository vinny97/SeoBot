import { CheckCircle2, ShieldCheck } from "lucide-react";
import { Card, PageHeader } from "@/components/ui";
import { JobItem, OpportunityCard, SectionHeader } from "@/components/foundation";
import { dashboardMock } from "@/lib/mock/dashboard";

export default function DashboardPage(){return <><PageHeader eyebrow="Good morning" title="Here is what your SEO employee is working on." description={`${dashboardMock.website} · Demonstration workspace`}/><div className="grid gap-8 xl:grid-cols-[minmax(0,1.25fr)_minmax(280px,.75fr)]"><div className="space-y-9"><section><SectionHeader title="Current work" description="Plain-English progress, without invented performance numbers."/><Card className="divide-y divide-[var(--border)]">{dashboardMock.currentWork.map(item=><JobItem key={item.id} {...item}/>)}</Card></section><section><SectionHeader title="Opportunities" description="Qualitative ideas to validate when real data becomes available."/><div className="grid gap-4 lg:grid-cols-2">{dashboardMock.opportunities.map(item=><OpportunityCard key={item.id} {...item}/>)}</div></section><section><SectionHeader title="Recently completed"/><Card className="divide-y divide-[var(--border)]">{dashboardMock.completed.map(item=><div key={item.id} className="flex gap-3 p-5"><CheckCircle2 size={20} className="mt-0.5 shrink-0 text-[var(--success)]"/><div><h3 className="font-semibold">{item.title}</h3><p className="mt-1 text-sm text-[var(--muted)]">{item.description}</p></div></div>)}</Card></section></div><aside className="space-y-7"><section><SectionHeader title="Approval"/><Card className="p-6 text-center"><ShieldCheck className="mx-auto text-[var(--success)]" size={28}/><h3 className="mt-3 font-semibold">Nothing needs your approval right now.</h3><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Future drafts and proposed changes will appear here before anything moves forward.</p></Card></section><section><SectionHeader title="Upcoming work"/><Card className="divide-y divide-[var(--border)]">{dashboardMock.upcoming.map((item,index)=><div key={item} className="flex gap-3 p-4"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-[var(--accent-soft)] text-xs font-bold text-[var(--accent)]">{index+1}</span><p className="text-sm font-medium leading-6">{item}</p></div>)}</Card></section></aside></div></>}
