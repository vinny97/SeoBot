import { lookup } from "node:dns/promises";
import { isIP } from "node:net";
export function isPrivateAddress(ip:string){if(ip.includes(":"))return ip==="::1"||ip.startsWith("fc")||ip.startsWith("fd")||ip.startsWith("fe80:");const parts=ip.split(".").map(Number);return parts[0]===10||parts[0]===127||parts[0]===0||parts[0]===169&&parts[1]===254||parts[0]===192&&parts[1]===168||parts[0]===172&&parts[1]>=16&&parts[1]<=31}
export async function validatePublicDestination(target:string){const parsed=new URL(target);const hostname=parsed.hostname.replace(/^\[|\]$/g,"");if(parsed.protocol!=="https:"&&parsed.protocol!=="http:")throw new Error("Only public HTTP websites can be analysed.");if(isIP(hostname)&&isPrivateAddress(hostname))throw new Error("Private network addresses are not allowed.");const addresses=await lookup(hostname,{all:true});if(!addresses.length||addresses.some(address=>isPrivateAddress(address.address)))throw new Error("This address cannot be analysed safely.")}
