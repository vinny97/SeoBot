export function calculateIndexability(input:{status:number;contentType:string;robotsAllowed:boolean;robotsMeta:string[];xRobotsTag:string[];canonicalUrl:string|null;finalUrl:string;fetchError?:string}){if(input.fetchError)return{indexable:false,reason:"Not evaluated because fetching failed",code:"fetch_failed"};if(!input.robotsAllowed)return{indexable:false,reason:"Not evaluated because robots.txt blocked crawling",code:"robots_blocked"};if(input.status>=300&&input.status<400)return{indexable:false,reason:"Redirects to another URL",code:"redirect"};if(input.status!==200)return{indexable:false,reason:`HTTP status ${input.status} is not a successful page response`,code:"http_status"};if(!["text/html","application/xhtml+xml"].includes(input.contentType))return{indexable:false,reason:"The response is not an HTML page",code:"non_html"};if(input.robotsMeta.includes("noindex"))return{indexable:false,reason:"Excluded by meta noindex",code:"meta_noindex"};if(input.xRobotsTag.some(value=>value.includes("noindex")))return{indexable:false,reason:"Excluded by X-Robots-Tag",code:"header_noindex"};if(input.canonicalUrl&&input.canonicalUrl!==input.finalUrl)return{indexable:false,reason:"Canonical points to another page",code:"canonical_elsewhere"};return{indexable:true,reason:"Indexable based on current crawl signals",code:"indexable"}}
