export type AgentStatus="idle"|"analysing_website"|"syncing_search_data"|"comparing_opportunities"|"preparing_recommendation"|"waiting_for_user"|"measuring"|"error";
export function deriveAgentStatus(input:{jobs:Array<{jobType:string;status:string}>;connectionStatus:string|null;hasBestRecommendation:boolean;hasApprovedWork:boolean;hasError:boolean}):AgentStatus{if(input.hasError||input.connectionStatus==="error"||input.connectionStatus==="needs_reauthentication")return"error";const active=input.jobs.filter(job=>["queued","running"].includes(job.status));if(active.some(job=>job.jobType==="website_crawl"||job.jobType==="page_intelligence_analysis"))return"analysing_website";if(active.some(job=>job.jobType.startsWith("gsc_")))return"syncing_search_data";if(active.some(job=>job.jobType==="generate_seo_recommendations"||job.jobType==="refresh_seo_recommendations"))return"comparing_opportunities";if(input.hasApprovedWork)return"measuring";if(input.hasBestRecommendation)return"waiting_for_user";if(input.connectionStatus==="connected")return"preparing_recommendation";return"idle"}
